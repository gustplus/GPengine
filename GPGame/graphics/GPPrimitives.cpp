#include "GPPrimitives.h"

using namespace GPEngine3D;

// GLuint Primitives::createCube(const vec3f &pos, float halfSize)
// {
	// GLfloat cube[] = {  pos.x - halfSize, pos.y + halfSize, pos.z + halfSize, -1, 1,
	// 					pos.x - halfSize, pos.y - halfSize, pos.z + halfSize, -1, -1,
	// 					pos.x + halfSize, pos.y + halfSize, pos.z + halfSize, 1, 1,
	// 					pos.x + halfSize, pos.y - halfSize, pos.z + halfSize, 1, -1, 1,

	// 					pos.x - halfSize, pos.y + halfSize, pos.z - halfSize,-1, 1, 
	// 					pos.x - halfSize, pos.y - halfSize, pos.z - halfSize, -1, -1, 
	// 					pos.x + halfSize, pos.y + halfSize, pos.z - halfSize, 1, 1, 
	// 					pos.x + halfSize, pos.y - halfSize, pos.z - halfSize, 1, -1, 
	// 					};
	// GLuint cubeHandle;
	// glGenVertexArrays(1, &cubeHandle);
	// glBindVertexArray(cubeHandle);
	// glGen
// }